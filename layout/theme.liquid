<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ page_title }}</title>

        {%- render 'stylesheets' -%}
        
        {{ content_for_header }}
    </head>

    <body>
        {{ content_for_layout }}
    </body>
    
</html>